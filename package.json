{
  "name": "barcode-metascan",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "core",
    "ui_web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w core\" \"npm run dev -w ui_web\"",
    "build": "npm run build -w core && npm run build -w ui_web",
    "test": "npm run test -w core && npm run test -w ui_web",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
